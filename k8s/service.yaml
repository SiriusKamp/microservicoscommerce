apiVersion: v1
kind: Service
metadata:
  name: cloud-commerce
spec:
  type: LoadBalancer
  selector:
    app: cloud-commerce
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
